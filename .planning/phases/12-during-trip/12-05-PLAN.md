---
phase: 12-during-trip
plan: 05
type: execute
wave: 3
depends_on: ["12-02", "12-03", "12-04"]
files_modified:
  - mobile/app/(app)/trip/[tripId]/index.tsx
  - mobile/app/(app)/trip/[tripId]/_layout.tsx
autonomous: false

must_haves:
  truths:
    - "Guest can access all during-trip features from trip overview"
    - "SOS button is prominently displayed on trip screen"
    - "Navigation to all Phase 12 screens works correctly"
  artifacts:
    - path: "mobile/app/(app)/trip/[tripId]/index.tsx"
      provides: "Trip overview with SOS button and feature links"
      contains: "SOSButton"
    - path: "mobile/app/(app)/trip/[tripId]/_layout.tsx"
      provides: "Navigation configuration for all trip screens"
      min_lines: 20
  key_links:
    - from: "mobile/app/(app)/trip/[tripId]/index.tsx"
      to: "all Phase 12 screens"
      via: "router.push navigation"
      pattern: "router\\.push"
---

<objective>
Wire up trip overview with SOS button and navigation to all Phase 12 features

Purpose: Provide a cohesive trip experience where guests can easily access all during-trip features
Output: Updated trip overview with feature cards and SOS button, verified navigation
</objective>

<execution_context>
@/Users/grantcharge/.claude/get-shit-done/workflows/execute-plan.md
@/Users/grantcharge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-during-trip/12-02-SUMMARY.md
@.planning/phases/12-during-trip/12-03-SUMMARY.md
@.planning/phases/12-during-trip/12-04-SUMMARY.md

# Existing trip overview
@mobile/app/(app)/trip/[tripId]/index.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SOS button and feature cards to trip overview</name>
  <files>mobile/app/(app)/trip/[tripId]/index.tsx</files>
  <action>
**Update trip overview (index.tsx):**

1. **Add SOS Button (prominent placement):**
   - Import SOSButton from components/sos/SOSButton
   - Place in fixed position at bottom-right or as floating action button
   - Style: Always visible, red background, accessible touch target
   - Position: absolute bottom-4 right-4 (or similar)

2. **Add During-Trip Feature Cards section:**
   After existing content (countdown, checklist, etc.), add a new section:

   ```typescript
   // "During Your Trip" section
   <Text className="text-lg font-semibold mb-3">During Your Trip</Text>
   <View className="flex-row flex-wrap gap-3">
     <FeatureCard
       icon="calendar"
       title="Itinerary"
       subtitle="View daily activities"
       onPress={() => router.push(`/trip/${tripId}/itinerary`)}
     />
     <FeatureCard
       icon="chat"
       title="Concierge"
       subtitle="24/7 support"
       onPress={() => router.push(`/trip/${tripId}/concierge`)}
     />
     <FeatureCard
       icon="tennis"
       title="Book Courts"
       subtitle="Reserve pickleball time"
       onPress={() => router.push(`/trip/${tripId}/courts`)}
     />
     <FeatureCard
       icon="users"
       title="Find Players"
       subtitle="Connect with guests"
       onPress={() => router.push(`/trip/${tripId}/players`)}
     />
     <FeatureCard
       icon="car"
       title="Transportation"
       subtitle="Request a ride"
       onPress={() => router.push(`/trip/${tripId}/transport`)}
     />
     <FeatureCard
       icon="camera"
       title="Photo Gallery"
       subtitle="Trip memories"
       onPress={() => router.push(`/trip/${tripId}/photos`)}
     />
     <FeatureCard
       icon="book"
       title="My Journal"
       subtitle="Your photos"
       onPress={() => router.push(`/trip/${tripId}/journal`)}
     />
   </View>
   ```

3. **Create FeatureCard component (inline or separate file):**
   - Props: { icon, title, subtitle, onPress }
   - Display: Icon, title, subtitle in a tappable card
   - Style: bg-white rounded-lg p-4 shadow-sm, 2-column grid width

Note: Use appropriate icons from @expo/vector-icons (Ionicons or similar).
  </action>
  <verify>
- TypeScript compiles
- SOS button renders on trip overview
- Feature cards render and are tappable
  </verify>
  <done>Trip overview updated with SOS button and feature card navigation</done>
</task>

<task type="auto">
  <name>Task 2: Verify navigation layout and all routes</name>
  <files>mobile/app/(app)/trip/[tripId]/_layout.tsx</files>
  <action>
**Verify _layout.tsx configuration:**

Ensure all Phase 12 screens are properly routed. With Expo Router file-based routing, screens should auto-register, but verify:

1. Check that _layout.tsx uses Stack navigator (recommended for trip details)
2. Ensure all screen options are configured:

```typescript
import { Stack } from 'expo-router';

export default function TripLayout() {
  return (
    <Stack>
      <Stack.Screen name="index" options={{ title: 'Trip Overview' }} />
      <Stack.Screen name="itinerary" options={{ title: 'Itinerary' }} />
      <Stack.Screen name="chat" options={{ title: 'Group Chat' }} />
      <Stack.Screen name="concierge" options={{ title: 'Concierge Support' }} />
      <Stack.Screen name="travelers" options={{ title: 'Fellow Travelers' }} />
      <Stack.Screen name="packing" options={{ title: 'Packing List' }} />
      <Stack.Screen name="courts" options={{ title: 'Book Courts' }} />
      <Stack.Screen name="players" options={{ title: 'Find Players' }} />
      <Stack.Screen name="transport" options={{ title: 'Transportation' }} />
      <Stack.Screen name="photos" options={{ title: 'Trip Photos' }} />
      <Stack.Screen name="journal" options={{ title: 'My Journal' }} />
    </Stack>
  );
}
```

3. Add headerRight SOS button option to Stack.Screen components where appropriate (optional enhancement - SOS accessible from any screen)
  </action>
  <verify>
- Navigation between all screens works
- Back button returns to trip overview
- Screen titles display correctly
  </verify>
  <done>Navigation layout configured for all Phase 12 screens</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 12 During-Trip Experience with all features wired together</what-built>
  <how-to-verify>
1. Start the mobile app: `cd mobile && npx expo start`
2. Log in and navigate to a trip with a confirmed booking
3. Verify trip overview shows:
   - SOS button (red, prominent)
   - Feature cards for all during-trip features
4. Test each navigation:
   - Tap "Itinerary" -> verify check-in buttons on activities
   - Tap "Concierge" -> verify chat interface loads
   - Tap "Book Courts" -> verify form renders
   - Tap "Find Players" -> verify travelers list
   - Tap "Transportation" -> verify form renders
   - Tap "Photo Gallery" -> verify grid renders
   - Tap "My Journal" -> verify upload component
5. Test SOS flow:
   - Tap SOS button -> modal opens
   - Grant location permission (or deny to test fallback)
   - Tap "Send SOS" -> verify alert confirmation
6. Back navigation works from all screens
  </how-to-verify>
  <resume-signal>Type "approved" if all navigation and features work, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:
1. All Phase 12 screens accessible from trip overview
2. SOS button visible and functional
3. Navigation between screens works correctly
4. Back button returns to expected screens
</verification>

<success_criteria>
- Trip overview is the hub for all during-trip features
- SOS button prominently displayed and functional
- All Phase 12 screens accessible via feature cards
- Navigation flows smoothly between screens
- Human verification confirms end-to-end experience works
</success_criteria>

<output>
After completion, create `.planning/phases/12-during-trip/12-05-SUMMARY.md`
</output>
