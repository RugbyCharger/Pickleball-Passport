---
phase: 06-gift-recipient-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Gift notification email is sent when gift transitions to SENT state"
    - "Email contains package details, personal message, and acceptance link"
    - "Scheduled gifts are sent by cron at delivery date"
    - "Acceptance page displays gift details correctly"
    - "Login/signup flows work on acceptance page"
    - "Accepting gift transfers booking ownership to recipient"
    - "Both purchaser and recipient receive confirmation emails on acceptance"
  artifacts:
    - path: "lib/gift/gift-transition-service.ts"
      provides: "handleSentTransition sends notification email"
      contains: "generateGiftNotificationRecipientEmail"
    - path: "lib/email/templates/gift-notification-recipient.ts"
      provides: "SENT notification email template"
      exports: ["generateGiftNotificationRecipientEmail"]
    - path: "app/api/cron/send-scheduled-gifts/route.ts"
      provides: "Cron job for scheduled gift sending"
      exports: ["GET"]
    - path: "app/gift/accept/page.tsx"
      provides: "Gift acceptance UI"
      contains: "GiftAcceptPage"
    - path: "lib/email/templates/gift-acceptance-confirmation-recipient.ts"
      provides: "Acceptance confirmation to recipient"
      exports: ["generateGiftAcceptanceConfirmationRecipientEmail"]
    - path: "lib/email/templates/gift-acceptance-notification-purchaser.ts"
      provides: "Acceptance notification to purchaser"
      exports: ["generateGiftAcceptanceNotificationPurchaserEmail"]
  key_links:
    - from: "gift-transition-service.ts"
      to: "gift-notification-recipient.ts"
      via: "handleSentTransition imports and calls"
      pattern: "generateGiftNotificationRecipientEmail"
    - from: "gift-transition-service.ts"
      to: "sendgrid.ts"
      via: "sendEmail function call"
      pattern: "sendEmail\\("
    - from: "app/gift/accept/page.tsx"
      to: "lib/trpc/server/routers/gift.ts"
      via: "trpc.gift.getByToken and acceptGift"
      pattern: "trpc\\.gift\\."
---

<objective>
Verify that existing gift recipient functionality works correctly for GIFT-07 through GIFT-13.

Purpose: Research indicates 80% of Phase 6 is already built. This plan validates that claim by testing each requirement end-to-end before proceeding with gap closure work. Verification prevents building duplicate functionality.

Output: Verified functionality documented in SUMMARY, or specific gaps identified for follow-up plans.
</objective>

<execution_context>
@/Users/grantcharge/.claude/get-shit-done/workflows/execute-plan.md
@/Users/grantcharge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-gift-recipient-experience/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify gift notification email flow (GIFT-07, GIFT-08)</name>
  <files>lib/gift/gift-transition-service.ts, lib/email/templates/gift-notification-recipient.ts</files>
  <action>
Read and trace the SENT transition flow to verify:

1. In `gift-transition-service.ts`, find `handleSentTransition()` and confirm:
   - It calls `generateGiftNotificationRecipientEmail()` with correct data
   - It calls `sendEmail()` with recipient email, subject, html, and text
   - It logs success/failure

2. In `gift-notification-recipient.ts`, verify the template includes:
   - Recipient first name greeting
   - Purchaser name (who sent the gift)
   - Package name and details (duration, accommodation tier)
   - Trip dates if available
   - Total value formatted as currency
   - Personal message section (giftMessage)
   - Acceptance URL with token
   - Add-ons list if present

3. Check that acceptanceUrl is constructed correctly:
   `${baseUrl}/gift/accept?token=${booking.giftAcceptanceToken}`

Document findings: What works, what's missing, any issues found.
  </action>
  <verify>Code tracing confirms all GIFT-07 and GIFT-08 requirements are implemented</verify>
  <done>SENT email flow verified: notification triggers on transition, email contains all required elements</done>
</task>

<task type="auto">
  <name>Task 2: Verify scheduled gift cron and acceptance flow (GIFT-09, GIFT-10, GIFT-11, GIFT-12, GIFT-13)</name>
  <files>app/api/cron/send-scheduled-gifts/route.ts, app/gift/accept/page.tsx, lib/trpc/server/routers/gift.ts</files>
  <action>
Read and trace these flows:

1. **GIFT-09 - Scheduled gifts cron** (`send-scheduled-gifts/route.ts`):
   - Verify it finds PENDING gifts where `giftDeliveryDate <= now`
   - Verify it calls `transitionGiftState(bookingId, GiftState.SENT, 'cron')`
   - Check cron schedule in `vercel.json` (should be configured)

2. **GIFT-10 - Gift details display** (`app/gift/accept/page.tsx`):
   - Verify `trpc.gift.getByToken` is called with token from URL
   - Verify gift details are rendered: package name, duration, accommodation, trip dates, add-ons, total value, personal message, purchaser name

3. **GIFT-11 - Login/signup flows** (`app/gift/accept/page.tsx`):
   - Verify "I Already Have an Account" button shows Clerk SignIn
   - Verify "Create My Account" button shows Clerk SignUp
   - Verify SignUp pre-fills recipient email, first name, last name
   - Verify redirect URLs include token

4. **GIFT-12 - Ownership transfer** (`gift.ts` + `gift-transition-service.ts`):
   - Verify `acceptGift` mutation validates email match
   - Verify `transitionGiftState` with ACCEPTED updates `booking.user`
   - Verify `giftAcceptedAt` is set

5. **GIFT-13 - Confirmation emails** (`gift-transition-service.ts`):
   - Verify `handleAcceptedTransition` sends email to recipient
   - Verify `handleAcceptedTransition` sends email to purchaser
   - Check both email templates exist and have content

Document findings for each requirement.
  </action>
  <verify>All five requirements (GIFT-09 through GIFT-13) traced and verified in code</verify>
  <done>Acceptance flow verified: cron triggers sends, page displays details, auth works, ownership transfers, both parties receive emails</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Verified existing gift recipient functionality through code analysis</what-built>
  <how-to-verify>
Review the verification findings. If all requirements are confirmed working:

1. Confirm GIFT-07 through GIFT-13 are marked as VERIFIED
2. Confirm no gaps were found that require additional implementation
3. Approve to proceed with remaining plans (decline page and email templates)

If gaps were found:
1. List the specific issues discovered
2. These will be addressed in follow-up gap closure plans
  </how-to-verify>
  <resume-signal>Type "verified" to confirm functionality works, or describe any gaps found</resume-signal>
</task>

</tasks>

<verification>
- GIFT-07: handleSentTransition sends email on SENT transition
- GIFT-08: Email template contains package, message, accept link
- GIFT-09: Cron job finds and sends scheduled gifts
- GIFT-10: Accept page displays all gift details
- GIFT-11: Login/signup flows work with pre-filled data
- GIFT-12: Acceptance transfers booking ownership
- GIFT-13: Both parties receive confirmation emails
</verification>

<success_criteria>
- All seven requirements (GIFT-07 through GIFT-13) verified as implemented
- No blocking gaps identified
- Documentation of what each piece does ready for SUMMARY
</success_criteria>

<output>
After completion, create `.planning/phases/06-gift-recipient-experience/06-01-SUMMARY.md`
</output>
