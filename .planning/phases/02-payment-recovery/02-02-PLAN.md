---
phase: 02-payment-recovery
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true

must_haves:
  truths:
    - "Guest with failed installment can click 'Update Payment Method' in dashboard"
    - "Payment method update modal integrates with Stripe for secure card replacement"
    - "New card can be entered and saved successfully"
  artifacts:
    - path: "components/booking/update-payment-method-modal.tsx"
      provides: "Stripe Elements modal for card update"
      exports: ["UpdatePaymentMethodModal"]
    - path: "components/booking/update-payment-method-button.tsx"
      provides: "Button to trigger modal"
      exports: ["UpdatePaymentMethodButton"]
  key_links:
    - from: "components/booking/update-payment-method-button.tsx"
      to: "components/booking/update-payment-method-modal.tsx"
      via: "renders modal on click"
      pattern: "UpdatePaymentMethodModal"
    - from: "components/booking/update-payment-method-modal.tsx"
      to: "trpc.payment.createSetupIntent"
      via: "tRPC mutation for Stripe SetupIntent"
      pattern: "createSetupIntent"
---

<objective>
Verify existing payment method update UI is complete and functional.

Purpose: Research confirmed PAY-02 and PAY-03 are already implemented. This plan verifies the existing UpdatePaymentMethodModal works correctly and identifies any edge cases needing attention.

Output: Verification that existing components satisfy requirements. No code changes expected.
</objective>

<execution_context>
@/Users/grantcharge/.claude/get-shit-done/workflows/execute-plan.md
@/Users/grantcharge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-payment-recovery/02-RESEARCH.md

# Existing implementation to verify
@components/booking/update-payment-method-modal.tsx
@components/booking/update-payment-method-button.tsx
@app/(dashboard)/dashboard/bookings/[id]/page.tsx (where button is rendered)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify UpdatePaymentMethodModal implementation</name>
  <files>components/booking/update-payment-method-modal.tsx</files>
  <action>
Read and verify the modal component has all required functionality:

1. Stripe Elements integration:
   - Uses PaymentElement for secure card input
   - Wraps in Elements provider with clientSecret

2. SetupIntent flow:
   - Calls trpc.payment.createSetupIntent on modal open
   - Handles clientSecret state management
   - Calls stripe.confirmSetup with redirect: 'if_required'

3. Payment method update:
   - Calls trpc.payment.updateDefaultPaymentMethod with new paymentMethodId
   - Handles success state with confirmation message
   - Handles error state with retry option

4. UX requirements:
   - Shows current payment method on file (if available)
   - Loading state while fetching SetupIntent
   - Error handling with user-friendly messages
   - Success confirmation before closing

If any of these are missing, note them. If all present, confirm component is COMPLETE.
  </action>
  <verify>
Read file and verify each checkpoint in the code.
Run: `npx tsc --noEmit components/booking/update-payment-method-modal.tsx`
  </verify>
  <done>
UpdatePaymentMethodModal verified as complete with Stripe Elements, SetupIntent flow, and proper state handling.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify button visibility for failed payments</name>
  <files>components/booking/update-payment-method-button.tsx, app/(dashboard)/dashboard/bookings/[id]/page.tsx</files>
  <action>
Verify the UpdatePaymentMethodButton is rendered for bookings with failed payments:

1. Check UpdatePaymentMethodButton component:
   - Accepts bookingId and bookingReference props
   - Renders button that opens modal
   - Has appropriate styling (visible, accessible)

2. Check booking detail page integration:
   - Locate where UpdatePaymentMethodButton is rendered
   - Verify condition for showing button (should show when booking has installment plan with Stripe customer)
   - Confirm button is visible for FAILED payment status

3. Check PaymentScheduleDisplay component (if used):
   - Verify it shows OVERDUE/FAILED status clearly
   - Verify visual indicator for failed payments

If button visibility condition needs adjustment for FAILED status (not just OVERDUE), note this as a gap.
  </action>
  <verify>
Grep for UpdatePaymentMethodButton usage in the codebase.
Verify TypeScript compiles: `npx tsc --noEmit components/booking/update-payment-method-button.tsx`
  </verify>
  <done>
Button visibility verified - renders for installment bookings with Stripe customer. FAILED status handling confirmed.
  </done>
</task>

</tasks>

<verification>
1. Modal component has all 4 functionality areas verified
2. Button is rendered in booking detail page
3. TypeScript compiles without errors
4. No missing functionality identified (or gaps documented)
</verification>

<success_criteria>
- [ ] UpdatePaymentMethodModal verified complete
- [ ] Stripe Elements integration confirmed
- [ ] SetupIntent flow confirmed working
- [ ] Button visibility for failed payments confirmed
- [ ] No code changes needed (or gaps documented for follow-up)
</success_criteria>

<output>
After completion, create `.planning/phases/02-payment-recovery/02-02-SUMMARY.md`

Note: If verification finds gaps, document them in SUMMARY.md and create a follow-up task.
</output>
