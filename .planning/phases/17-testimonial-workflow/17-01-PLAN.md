---
phase: 17-testimonial-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/testimonials/testimonial-gallery.tsx
  - lib/storage/testimonial-storage.ts
autonomous: false

must_haves:
  truths:
    - "Guest can submit testimonial via web dashboard at /dashboard/testimonials"
    - "Admin can view pending testimonials and approve/reject/request edits"
    - "Approved and published testimonials display on public /testimonials page"
    - "Mobile app testimonial submission works via TestimonialForm component"
    - "File uploads work for photos (before/after) via Supabase Storage"
  artifacts:
    - path: "components/testimonials/testimonial-gallery.tsx"
      provides: "Public testimonials display using real data from database"
      min_lines: 100
    - path: "lib/storage/testimonial-storage.ts"
      provides: "File upload helpers for Supabase Storage"
      exports: ["validateTestimonialFile", "TESTIMONIAL_FILE_LIMITS"]
  key_links:
    - from: "components/testimonials/testimonial-gallery.tsx"
      to: "trpc.guestTestimonial.getPublished"
      via: "tRPC query"
      pattern: "trpc\\.guestTestimonial\\.getPublished"
    - from: "components/testimonials/testimonial-submission-form.tsx"
      to: "Supabase Storage"
      via: "file upload with signed URLs"
      pattern: "supabase.*storage.*upload"
---

<objective>
Verify and complete the testimonial workflow integration, ensuring guest submission, admin moderation, and public display all work end-to-end.

Purpose: Epic 12-5 delivered a production-ready testimonial system with complete database schema, tRPC API, admin UI, submission forms, and mobile app components. This plan verifies existing functionality and completes missing integrations (file uploads, public gallery with real data).

Output: Fully operational testimonial workflow where guests can submit testimonials via web or mobile, admins can moderate them, and approved testimonials display on the public website.
</objective>

<execution_context>
@/Users/grantcharge/.claude/get-shit-done/workflows/execute-plan.md
@/Users/grantcharge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-testimonial-workflow/17-RESEARCH.md
@lib/trpc/server/routers/guest-testimonial.ts
@components/testimonials/testimonial-submission-form.tsx
@components/testimonials/testimonial-gallery.tsx
@app/(dashboard)/dashboard/admin/cms/testimonials/page.tsx
@app/(dashboard)/dashboard/testimonials/page.tsx
@app/(marketing)/testimonials/page.tsx
@mobile/app/(app)/alumni/testimonial.tsx
@mobile/components/alumni/TestimonialForm.tsx
</context>

<tasks>

<task type="auto">
  <name>Update public gallery to fetch real testimonials from database</name>
  <files>components/testimonials/testimonial-gallery.tsx</files>
  <action>
Replace placeholder testimonials in TestimonialGallery component with real data from database via tRPC.

Current implementation uses hardcoded `placeholderTestimonials` array. Replace with:
- Import `trpc` client from '@/lib/trpc/client'
- Use `trpc.guestTestimonial.getPublished.useQuery()` to fetch published testimonials
- Support filtering by packageType (if packageType prop provided, pass to query)
- Handle loading state with skeleton cards
- Handle empty state with "No testimonials yet" message
- Map database fields to gallery card display:
  - guestName -> name
  - guestLocation -> location
  - content -> quote
  - packageType -> package badge
  - publishedAt -> date
  - videoUrl/beforePhotoUrl/afterPhotoUrl -> media indicators

Preserve existing filtering UI (packageType filter, showFilters prop).

Why: Public testimonials page currently shows placeholder data instead of actual guest testimonials from database. Research confirmed the tRPC procedure exists (getPublished in guest-testimonial.ts) but gallery component doesn't use it.
  </action>
  <verify>Visit http://localhost:3000/testimonials in browser, inspect network tab to confirm tRPC query fires and returns published testimonials</verify>
  <done>Public testimonials page fetches and displays real testimonials from database, or shows "No testimonials yet" if none published</done>
</task>

<task type="auto">
  <name>Verify file upload integration with Supabase Storage</name>
  <files>lib/storage/testimonial-storage.ts</files>
  <action>
Verify testimonial-storage.ts implements actual file upload to Supabase Storage, not just validation helpers.

Expected implementation:
- Export `uploadTestimonialFile(file: File, type: 'video' | 'photo'): Promise<string>` function
- Use Supabase Storage client from '@/lib/supabase-client'
- Upload to 'testimonials' bucket with path structure: `testimonials/{userId}/{timestamp}-{filename}`
- Validate file before upload using existing `validateTestimonialFile` helper
- Return public URL for uploaded file
- Handle upload errors with TRPCError

If uploadTestimonialFile function doesn't exist, add it. If Supabase Storage client doesn't exist at @/lib/supabase-client, create it using @supabase/supabase-js with:
- NEXT_PUBLIC_SUPABASE_URL from env
- NEXT_PUBLIC_SUPABASE_ANON_KEY from env

Testimonial submission form already has file upload handlers (videoFile, beforePhoto, afterPhoto state) but currently expects URL inputs. Update form to:
1. Upload file to Supabase Storage using uploadTestimonialFile
2. Pass returned URL to tRPC mutation
3. Show upload progress indicator

Why: Research noted "file upload to Supabase Storage is connected (current implementation has placeholder)" - need to verify and complete actual upload implementation.
  </action>
  <verify>
Run `npm run dev`, log in as guest, navigate to /dashboard/testimonials, select "Before/After" testimonial type, attempt to upload before/after photos, check browser console for Supabase Storage upload requests
  </verify>
  <done>File uploads work for testimonial photos and videos, storing files in Supabase Storage and returning URLs for database storage</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete testimonial workflow with:
- Guest submission form (web + mobile)
- Admin moderation UI
- Public testimonials gallery with real data
- File upload to Supabase Storage
  </what-built>
  <how-to-verify>
Test the full workflow end-to-end:

1. **Guest Submission (Web)**
   - Visit http://localhost:3000/dashboard/testimonials
   - Click "Submit Testimonial"
   - Select "Before/After" type
   - Upload before photo and after photo
   - Fill in name, location, testimonial text
   - Check consent checkbox
   - Submit
   - Verify success message appears
   - Verify testimonial appears in "My Testimonials" list with PENDING status

2. **Admin Moderation**
   - Visit http://localhost:3000/dashboard/admin/cms/testimonials
   - Verify pending testimonial appears in queue
   - Click to view details
   - Click "Approve"
   - Verify status changes to APPROVED
   - Click "Publish"
   - Verify status changes to PUBLISHED

3. **Public Display**
   - Visit http://localhost:3000/testimonials
   - Verify published testimonial appears in gallery
   - Verify before/after photos display correctly
   - Verify testimonial content shows correctly

4. **Mobile App (if available)**
   - Open mobile app
   - Navigate to Alumni -> Testimonial
   - Attempt to submit testimonial
   - Verify submission works

Expected outcomes:
- Web submission works with file uploads
- Admin can approve and publish
- Published testimonials appear on public page
- Mobile submission works (if testing on mobile)
  </how-to-verify>
  <resume-signal>Type "approved" if all workflows work correctly, or describe any issues encountered</resume-signal>
</task>

</tasks>

<verification>
1. Run `npm run dev` and navigate to /testimonials - page loads without errors
2. Run `npm run dev` and navigate to /dashboard/testimonials - submission form works
3. Admin can access /dashboard/admin/cms/testimonials and perform moderation actions
4. tRPC query `getPublished` returns correct data
5. File uploads to Supabase Storage succeed
6. Mobile app TestimonialForm component submits via same tRPC API
</verification>

<success_criteria>
1. Guest can submit testimonial with text and photos via web dashboard
2. Uploaded files store in Supabase Storage and return URLs
3. Admin can view pending testimonials and perform approve/reject/request edits actions
4. Approved + published testimonials display on public /testimonials page
5. Mobile app testimonial submission works via shared tRPC API
6. No console errors during testimonial workflow
</success_criteria>

<output>
After completion, create `.planning/phases/17-testimonial-workflow/17-01-SUMMARY.md`
</output>
