---
phase: 18-security-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/cron/charge-installments/route.ts
  - app/api/cron/send-payment-reminders/route.ts
  - app/api/cron/send-pre-trip-emails/route.ts
  - app/api/cron/send-post-trip-emails/route.ts
  - app/api/cron/referral-completion-bonus/route.ts
  - app/api/cron/whatsapp-milestones/route.ts
  - app/api/cron/expire-gifts/route.ts
  - app/api/cron/send-scheduled-gifts/route.ts
autonomous: true

must_haves:
  truths:
    - "No console.log statements exist in cron job handlers"
    - "All cron logs use cronLogger from lib/logger"
    - "Error logs include structured error context"
  artifacts:
    - path: "app/api/cron/charge-installments/route.ts"
      provides: "Installment charging cron with structured logging"
      min_lines: 30
    - path: "app/api/cron/send-pre-trip-emails/route.ts"
      provides: "Pre-trip email cron with structured logging"
      min_lines: 30
  key_links:
    - from: "app/api/cron/*.ts"
      to: "lib/logger/index.ts"
      via: "import cronLogger"
      pattern: "cronLogger\\.(info|error|warn|debug)"
---

<objective>
Migrate all console.log statements in cron job handlers to pino structured logging.

Purpose: SEC-04 requires no sensitive data in console.log statements. Cron jobs are high-risk because they process batches of user data and errors could expose PII. The 8 cron files have 88+ console statements that need migration.

Output: Zero console.log in cron handlers, all logs use cronLogger with structured context.
</objective>

<execution_context>
@/Users/grantcharge/.claude/get-shit-done/workflows/execute-plan.md
@/Users/grantcharge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-security-hardening/18-RESEARCH.md
@lib/logger/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate charge-installments Cron Logging</name>
  <files>app/api/cron/charge-installments/route.ts</files>
  <action>
Replace all console.log/error statements with structured cronLogger and paymentLogger from lib/logger.

Add imports if not present:
```typescript
import { cronLogger, paymentLogger, stripeLogger } from '@/lib/logger';
```

Migration pattern:
- `console.log('Starting installment charging...')` -> `cronLogger.info('Starting installment charging cron')`
- `console.log(`Processing ${records.length} records`)` -> `cronLogger.info({ recordCount: records.length }, 'Processing installment records')`
- `console.log(`Charging booking ${bookingId}...`)` -> `paymentLogger.info({ bookingId, amount }, 'Charging installment')`
- `console.error('Failed to charge:', error)` -> `paymentLogger.error({ err: error, bookingId }, 'Failed to charge installment')`
- `console.log('Completed: ${success}/${total}')` -> `cronLogger.info({ success, failed, total }, 'Installment charging complete')`

Use structured objects for context: `{ bookingId, paymentRecordId, amount, installmentNumber }` instead of string interpolation.
  </action>
  <verify>
Run `grep -c "console\." app/api/cron/charge-installments/route.ts` - should return 0.
  </verify>
  <done>
All console statements in charge-installments/route.ts replaced with structured pino logging.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate Payment and Email Cron Logging</name>
  <files>
    app/api/cron/send-payment-reminders/route.ts
    app/api/cron/send-pre-trip-emails/route.ts
    app/api/cron/send-post-trip-emails/route.ts
  </files>
  <action>
Replace all console.log/error statements in these three email/payment cron handlers with structured logging.

For each file, add imports:
```typescript
import { cronLogger, emailLogger } from '@/lib/logger';
```

**send-payment-reminders/route.ts** migration:
- Job start/end -> `cronLogger.info({ jobName: 'payment-reminders' }, 'Starting payment reminder job')`
- Booking processing -> `emailLogger.info({ bookingId, dueDate }, 'Sending payment reminder')`
- Email sent -> `emailLogger.info({ bookingId, recipientCount: 1 }, 'Payment reminder sent')`
- Errors -> `emailLogger.error({ err: error, bookingId }, 'Failed to send payment reminder')`
- Summary -> `cronLogger.info({ sent, failed, total }, 'Payment reminders complete')`

**send-pre-trip-emails/route.ts** migration:
- Job start -> `cronLogger.info({ jobName: 'pre-trip-emails' }, 'Starting pre-trip email job')`
- Email sending -> `emailLogger.info({ bookingId, daysBeforeTrip }, 'Sending pre-trip email')`
- Errors -> `emailLogger.error({ err: error, bookingId }, 'Failed to send pre-trip email')`
- Summary -> `cronLogger.info({ sent, failed }, 'Pre-trip emails complete')`

**send-post-trip-emails/route.ts** migration:
- Job start -> `cronLogger.info({ jobName: 'post-trip-emails' }, 'Starting post-trip email job')`
- Email sending -> `emailLogger.info({ bookingId, daysAfterTrip }, 'Sending post-trip email')`
- Errors -> `emailLogger.error({ err: error, bookingId }, 'Failed to send post-trip email')`
- Summary -> `cronLogger.info({ sent, failed }, 'Post-trip emails complete')`

Pattern: Use descriptive message as second argument, context object as first argument.
  </action>
  <verify>
Run these commands - all should return 0:
- `grep -c "console\." app/api/cron/send-payment-reminders/route.ts`
- `grep -c "console\." app/api/cron/send-pre-trip-emails/route.ts`
- `grep -c "console\." app/api/cron/send-post-trip-emails/route.ts`
  </verify>
  <done>
All console statements in payment-reminders, pre-trip-emails, and post-trip-emails cron handlers replaced with structured pino logging.
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate Remaining Cron Job Logging</name>
  <files>
    app/api/cron/referral-completion-bonus/route.ts
    app/api/cron/whatsapp-milestones/route.ts
    app/api/cron/expire-gifts/route.ts
    app/api/cron/send-scheduled-gifts/route.ts
  </files>
  <action>
Replace all console.log/error statements in these four cron handlers with structured logging.

**referral-completion-bonus/route.ts** (20 console statements):
Add imports:
```typescript
import { cronLogger, partnerLogger } from '@/lib/logger';
```
- Job start -> `cronLogger.info({ jobName: 'referral-completion-bonus' }, 'Starting referral bonus job')`
- Partner processing -> `partnerLogger.info({ partnerId, referralCount }, 'Processing referral bonus')`
- Bonus awarded -> `partnerLogger.info({ partnerId, bonusAmount }, 'Referral bonus awarded')`
- Errors -> `partnerLogger.error({ err: error, partnerId }, 'Failed to process referral bonus')`

**whatsapp-milestones/route.ts** (12 console statements):
Add imports:
```typescript
import { cronLogger, whatsappLogger } from '@/lib/logger';
```
- Job start -> `cronLogger.info({ jobName: 'whatsapp-milestones' }, 'Starting WhatsApp milestone job')`
- Message sending -> `whatsappLogger.info({ bookingId, milestoneType }, 'Sending milestone message')`
- Errors -> `whatsappLogger.error({ err: error, bookingId }, 'Failed to send milestone message')`

**expire-gifts/route.ts** (2 console statements):
Add imports:
```typescript
import { cronLogger, giftLogger } from '@/lib/logger';
```
- Job processing -> `cronLogger.info({ jobName: 'expire-gifts', expiredCount }, 'Expired gifts processed')`
- Errors -> `giftLogger.error({ err: error }, 'Failed to expire gifts')`

**send-scheduled-gifts/route.ts** (2 console statements):
Add imports:
```typescript
import { cronLogger, giftLogger, emailLogger } from '@/lib/logger';
```
- Gift sending -> `giftLogger.info({ giftId, recipientEmail: '[REDACTED]' }, 'Sending scheduled gift')` (note: email will be auto-redacted by pino)
- Errors -> `giftLogger.error({ err: error, giftId }, 'Failed to send scheduled gift')`
  </action>
  <verify>
Run these commands - all should return 0:
- `grep -c "console\." app/api/cron/referral-completion-bonus/route.ts`
- `grep -c "console\." app/api/cron/whatsapp-milestones/route.ts`
- `grep -c "console\." app/api/cron/expire-gifts/route.ts`
- `grep -c "console\." app/api/cron/send-scheduled-gifts/route.ts`
  </verify>
  <done>
All console statements in referral-completion-bonus, whatsapp-milestones, expire-gifts, and send-scheduled-gifts cron handlers replaced with structured pino logging.
  </done>
</task>

</tasks>

<verification>
1. `grep -rn "console\." app/api/cron/` returns no matches in route.ts files
2. `grep -l "cronLogger" app/api/cron/*/route.ts | wc -l` equals 8 (all cron files import cronLogger)
3. Each cron file has proper imports: `import { cronLogger, ... } from '@/lib/logger'`
</verification>

<success_criteria>
- All 8 cron job handlers have 0 console.log/error/warn statements
- All cron files import and use cronLogger for job-level logging
- All cron files use appropriate module loggers (emailLogger, partnerLogger, etc.) for domain-specific logging
- Error logs include `{ err: error }` pattern for proper stack trace capture
</success_criteria>

<output>
After completion, create `.planning/phases/18-security-hardening/18-02-SUMMARY.md`
</output>
