---
phase: 18-security-hardening
plan: 04
type: execute
wave: 3
depends_on: [18-01, 18-02, 18-03]
files_modified:
  - app/api/webhooks/clerk/route.ts
  - app/api/receipts/[paymentId]/download/route.ts
  - app/api/trpc/[trpc]/route.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "No console.log statements exist in any API route files"
    - "All webhook logs use structured pino loggers"
    - "All error handling uses structured error context"
  artifacts:
    - path: "app/api/webhooks/clerk/route.ts"
      provides: "Clerk webhook with structured logging"
      min_lines: 30
    - path: "app/api/receipts/[paymentId]/download/route.ts"
      provides: "Receipt download with structured logging"
      min_lines: 20
  key_links:
    - from: "app/api/webhooks/clerk/route.ts"
      to: "lib/logger/index.ts"
      via: "import authLogger"
      pattern: "authLogger\\.(info|error|warn)"
---

<objective>
Migrate remaining 13 console.log/error statements to structured pino logging.

Purpose: Close SEC-04 gaps identified in VERIFICATION.md. These files were missed in the initial migration scope but need to be cleaned up for full compliance.

Output: Zero console.log statements in any API route file.
</objective>

<execution_context>
@/Users/grantcharge/.claude/get-shit-done/workflows/execute-plan.md
@/Users/grantcharge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-security-hardening/18-VERIFICATION.md
@lib/logger/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Clerk Webhook Logging</name>
  <files>app/api/webhooks/clerk/route.ts</files>
  <action>
Replace all 8 console.log/error statements with structured authLogger from lib/logger.

Import authLogger:
```typescript
import { authLogger } from '@/lib/logger';
```

Migration pattern:
- `console.log('User created:', userId)` -> `authLogger.info({ userId }, 'User created via Clerk webhook')`
- `console.log('User updated:', userId)` -> `authLogger.info({ userId }, 'User updated via Clerk webhook')`
- `console.error('Failed to sync user:', error)` -> `authLogger.error({ err: error, userId }, 'Failed to sync user from Clerk')`

Use structured objects for context. Email addresses will be auto-redacted by pino's PII redaction.
  </action>
  <verify>
Run `grep -c "console\." app/api/webhooks/clerk/route.ts` - should return 0.
  </verify>
  <done>
All 8 console statements in Clerk webhook replaced with structured authLogger calls.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate Receipt Download and tRPC Logging</name>
  <files>
    app/api/receipts/[paymentId]/download/route.ts
    app/api/trpc/[trpc]/route.ts
  </files>
  <action>
**app/api/receipts/[paymentId]/download/route.ts** (4 console.error):
Import and use pdfLogger or storageLogger:
```typescript
import { storageLogger } from '@/lib/logger';
```
- `console.error('Receipt not found')` -> `storageLogger.error({ paymentId }, 'Receipt not found')`
- `console.error('Failed to download receipt:', error)` -> `storageLogger.error({ err: error, paymentId }, 'Failed to download receipt')`

**app/api/trpc/[trpc]/route.ts** (1 console.error):
Import and use apiLogger:
```typescript
import { apiLogger } from '@/lib/logger';
```
- `console.error('tRPC error:', error)` -> `apiLogger.error({ err: error }, 'tRPC handler error')`
  </action>
  <verify>
Run these commands - both should return 0:
- `grep -c "console\." app/api/receipts/*/download/route.ts`
- `grep -c "console\." app/api/trpc/*/route.ts`
  </verify>
  <done>
All console statements in receipt download and tRPC handler replaced with structured logging.
  </done>
</task>

</tasks>

<verification>
1. `grep -rn "console\." app/api/webhooks/clerk/route.ts` returns no matches
2. `grep -rn "console\." app/api/receipts/` returns no matches
3. `grep -rn "console\." app/api/trpc/` returns no matches
4. `grep -c "console\." app/api/**/*.ts 2>/dev/null | grep -v ":0$"` shows no API route violations
</verification>

<success_criteria>
- app/api/webhooks/clerk/route.ts has 0 console statements
- app/api/receipts/[paymentId]/download/route.ts has 0 console statements
- app/api/trpc/[trpc]/route.ts has 0 console statements
- All structured logs use appropriate module loggers
</success_criteria>

<output>
After completion, create `.planning/phases/18-security-hardening/18-04-SUMMARY.md`
</output>
