---
phase: 10-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/package.json
  - mobile/app.json
  - mobile/tsconfig.json
  - mobile/app/_layout.tsx
  - mobile/app/index.tsx
  - mobile/.gitignore
  - mobile/babel.config.js
  - mobile/metro.config.js
  - mobile/tailwind.config.js
  - mobile/global.css
autonomous: true

must_haves:
  truths:
    - "Developer can run `npx expo start` in mobile/ directory"
    - "Developer can open app in iOS Simulator"
    - "Developer can open app in Android Emulator"
    - "TypeScript compilation succeeds with no errors"
  artifacts:
    - path: "mobile/package.json"
      provides: "Expo dependencies with exact version pinning"
      contains: "expo"
    - path: "mobile/app/_layout.tsx"
      provides: "Root layout with Expo Router"
      contains: "export default"
    - path: "mobile/tailwind.config.js"
      provides: "NativeWind configuration"
      contains: "nativewind"
  key_links:
    - from: "mobile/app/_layout.tsx"
      to: "mobile/global.css"
      via: "NativeWind import"
      pattern: "import.*global.css"
---

<objective>
Scaffold Expo React Native app with TypeScript, Expo Router, and NativeWind styling.

Purpose: Establish the mobile app foundation that all subsequent mobile features will build upon. This is the first step of the v2.0 Mobile App milestone.

Output: A runnable Expo app in the mobile/ subdirectory with file-based routing, TypeScript, and Tailwind-style classes via NativeWind.
</objective>

<execution_context>
@/Users/grantcharge/.claude/get-shit-done/workflows/execute-plan.md
@/Users/grantcharge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md

# Critical constraints from research:
# - Use Expo SDK 54 (stable, not SDK 55 beta)
# - Pin exact versions to prevent duplicate React issues
# - NativeWind v4 (not v5 pre-release)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Expo app with TypeScript and Expo Router</name>
  <files>
    mobile/package.json
    mobile/app.json
    mobile/tsconfig.json
    mobile/babel.config.js
    mobile/metro.config.js
    mobile/.gitignore
    mobile/app/_layout.tsx
    mobile/app/index.tsx
  </files>
  <action>
Create the mobile/ directory at /Users/grantcharge/Pickleball-Passport/mobile/

Run in the Pickleball-Passport root directory:
```
npx create-expo-app@latest mobile --template tabs
```

After creation, navigate to mobile/ and update package.json with exact version pins:
- Ensure React and React Native versions use exact pins (no ^ or ~)
- This prevents duplicate React issues in the project

Update app.json:
- Set name to "Pickleball Passport"
- Set slug to "pickleball-passport"
- Set scheme to "pickleballpassport" (for deep linking later)
- Set bundleIdentifier to "com.pickleballpassport.app" (iOS)
- Set package to "com.pickleballpassport.app" (Android)

Update tsconfig.json to enable strict mode and path aliases:
- Enable strict: true
- Add paths for @/* imports pointing to app/ directory

Verify Expo Router is configured in app/_layout.tsx:
- Should export default from expo-router
- Verify tabs navigation template is working
  </action>
  <verify>
Run in mobile/ directory:
- `npm install` succeeds
- `npx expo start` launches without errors
- Press 'i' to open iOS Simulator (must have Xcode installed)
- Press 'a' to open Android Emulator (must have Android Studio configured)
- Both simulators show the default tabs template
  </verify>
  <done>
Expo app runs on both iOS Simulator and Android Emulator showing tabs template.
TypeScript compiles without errors (`npx tsc --noEmit` passes).
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure NativeWind for Tailwind-style classes</name>
  <files>
    mobile/package.json
    mobile/tailwind.config.js
    mobile/global.css
    mobile/babel.config.js
    mobile/metro.config.js
    mobile/app/_layout.tsx
    mobile/nativewind-env.d.ts
  </files>
  <action>
Install NativeWind v4 and dependencies in mobile/:
```
npx expo install nativewind tailwindcss
```

Create tailwind.config.js in mobile/:
```js
/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./app/**/*.{js,jsx,ts,tsx}"],
  presets: [require("nativewind/preset")],
  theme: {
    extend: {},
  },
  plugins: [],
}
```

Create global.css in mobile/:
```css
@tailwind base;
@tailwind components;
@tailwind utilities;
```

Update babel.config.js to include NativeWind preset:
```js
module.exports = function (api) {
  api.cache(true);
  return {
    presets: [
      ["babel-preset-expo", { jsxImportSource: "nativewind" }],
      "nativewind/babel",
    ],
  };
};
```

Update metro.config.js for CSS support:
```js
const { getDefaultConfig } = require("expo/metro-config");
const { withNativeWind } = require("nativewind/metro");

const config = getDefaultConfig(__dirname);

module.exports = withNativeWind(config, { input: "./global.css" });
```

Create nativewind-env.d.ts for TypeScript support:
```ts
/// <reference types="nativewind/types" />
```

Update app/_layout.tsx to import global.css:
- Add `import "../global.css";` at the top of the file

Update app/index.tsx to use className instead of StyleSheet:
- Replace StyleSheet.create patterns with className="..."
- Example: `<View className="flex-1 items-center justify-center bg-white">`
  </action>
  <verify>
Run in mobile/:
- `npx expo start` launches without errors
- Open in iOS Simulator
- Tailwind classes render correctly (text colors, spacing, flexbox)
- No console warnings about NativeWind
  </verify>
  <done>
NativeWind styling works in mobile app.
className props apply Tailwind styles correctly.
App renders with styled components on both platforms.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create clean app structure with placeholder screens</name>
  <files>
    mobile/app/(auth)/_layout.tsx
    mobile/app/(auth)/sign-in.tsx
    mobile/app/(auth)/sign-up.tsx
    mobile/app/(app)/_layout.tsx
    mobile/app/(app)/(tabs)/_layout.tsx
    mobile/app/(app)/(tabs)/index.tsx
    mobile/app/(app)/(tabs)/bookings.tsx
    mobile/app/(app)/(tabs)/profile.tsx
    mobile/app/_layout.tsx
    mobile/app/index.tsx
  </files>
  <action>
Restructure the app/ directory for auth-aware routing:

1. Create auth group (public routes):
   - mobile/app/(auth)/_layout.tsx - Stack layout for auth screens
   - mobile/app/(auth)/sign-in.tsx - Placeholder sign-in screen
   - mobile/app/(auth)/sign-up.tsx - Placeholder sign-up screen

2. Create app group (protected routes):
   - mobile/app/(app)/_layout.tsx - Stack layout, will add auth protection later
   - mobile/app/(app)/(tabs)/_layout.tsx - Tab navigator
   - mobile/app/(app)/(tabs)/index.tsx - Dashboard/Home tab
   - mobile/app/(app)/(tabs)/bookings.tsx - Bookings tab
   - mobile/app/(app)/(tabs)/profile.tsx - Profile tab

3. Update root layout (mobile/app/_layout.tsx):
   - Import global.css
   - Set up Slot or Stack for route groups
   - Add SafeAreaProvider wrapper

4. Update mobile/app/index.tsx:
   - Redirect to (app)/(tabs) for now (will add auth check later)
   - Use `<Redirect href="/(app)/(tabs)" />` from expo-router

Each screen should:
- Use NativeWind classes for styling
- Display placeholder content with screen name
- Follow React Native best practices (SafeAreaView, etc.)

Tab bar configuration in (app)/(tabs)/_layout.tsx:
- Home icon for index (Dashboard)
- Calendar icon for bookings
- User icon for profile
- Use lucide-react-native for icons (install if needed)
  </action>
  <verify>
Run in mobile/:
- `npx expo start` launches without errors
- App shows tab navigation at bottom
- Tapping tabs switches between Dashboard, Bookings, Profile
- Each screen displays its placeholder content
- Navigation between auth and app groups works
  </verify>
  <done>
Mobile app has clean route structure with:
- (auth) group for sign-in/sign-up screens
- (app) group with tab navigation for Dashboard, Bookings, Profile
- All screens render with NativeWind styling
- Tab navigation works correctly
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run `cd mobile && npx expo start`
2. Press 'i' for iOS Simulator - app launches with tabs
3. Press 'a' for Android Emulator - app launches with tabs
4. Navigate between all tabs - smooth transitions
5. Run `npx tsc --noEmit` - no TypeScript errors
6. Verify mobile/ is a standalone directory (not monorepo)
</verification>

<success_criteria>
- MOB-SETUP-01 satisfied: Expo React Native app scaffolded with TypeScript
- Developer can build and run app on iOS Simulator
- Developer can build and run app on Android Emulator
- NativeWind styling works correctly
- Route structure prepared for auth flow in next plan
</success_criteria>

<output>
After completion, create `.planning/phases/10-foundation/10-01-SUMMARY.md`
</output>
