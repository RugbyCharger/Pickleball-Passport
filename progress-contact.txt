# Ralph Progress Log
Started: Thu Jan 22 09:30:00 +07 2026
---

## Codebase Patterns
- Admin pages are at app/(dashboard)/dashboard/admin/[feature]/page.tsx
- tRPC routers in lib/trpc/server/routers/
- Use adminProcedure for admin-only endpoints
- Use Recharts for charts and data visualization
- shadcn/ui components (Card, Button, Table, Dialog, Tabs, etc.)
- Use toast from 'sonner' for notifications
- Prisma for database models
- Date filtering patterns exist in admin dashboard

---

## Previous Epic Learnings
- E12 CMS: Most features were pre-implemented; focus on TypeScript fixes
- E12: Use `as const` for literal union types in return objects
- E12: Cast Prisma JsonValue through `unknown` when converting to typed interfaces
- E12: For readonly arrays, use `(arr as readonly string[]).includes()`
- E12: Use `!== undefined` instead of truthy check for conditional object spreads
- E4-S14: Use `logStripeError(error, message, context)` for Stripe errors
- E4-S14: No npm run typecheck script - use `npx tsc --noEmit` directly

---

