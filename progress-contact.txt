## Codebase Patterns
- Prisma schema uses cuid() for IDs
- Enum naming convention: CamelCase for enum types (e.g., GuestReferralStatus)
- Always add appropriate indexes for foreign keys and frequently queried fields
- User model uses Clerk user ID as @id (not cuid)
- Run `npx prisma generate` after schema changes
- Pre-existing TypeScript errors exist in test files - focus on new code compiling
- Pre-existing lint warnings exist (unused imports, no-img-element)
- Tests failing for STRIPE_SECRET_KEY are environment-related, not code issues

---

## 2026-01-21 - US-000: Prisma Schema Updates for Guest Referral System
Iteration: 1
- Implemented GuestReferralStatus enum (CLICKED, APPLIED, BOOKED, COMPLETED)
- Implemented ReferralEventType enum (CLICK, APPLICATION, BOOKING, COMPLETION)
- Added GuestReferral model with referrer/referred User relationships
- Added ReferralEvent model for tracking events with UTM params
- Added referralCode (unique, nullable) and referralPointsBalance to User model
- Added referralCodeClickCount to PartnerProfile model
- All new fields have appropriate indexes

Files changed:
- prisma/schema.prisma

**Learnings for future iterations:**
- User model uses Clerk ID as primary key (String @id), not auto-generated cuid
- GuestReferral uses @unique on referredUserId (one-to-one relationship)
- ReferralEvent is independent - no foreign key to User to allow anonymous tracking
- prisma db push requires DATABASE_URL and DIRECT_URL env vars (may fail locally)
- Cookie name for referrals: `referral_code` (used throughout the referral system)
- Next.js 14+ route params are async: use `{ params }: { params: Promise<{ code: string }> }` and `await params`
---

## 2026-01-21 - US-001: Referral Link Tracking (Cookie-Based)
Iteration: 2
- Created /r/[code] dynamic route at app/r/[code]/route.ts
- Sets referral_code cookie with 30-day expiration (httpOnly, secure, sameSite: lax)
- Stores click event in ReferralEvent table with IP address, user agent, UTM params
- Increments referralCodeClickCount on PartnerProfile for partner codes
- First-click attribution: checks existing cookie and doesn't overwrite
- Validates codes against both PartnerProfile.referralCode and User.referralCode
- Graceful error handling: invalid codes redirect to homepage without errors

Files changed:
- app/r/[code]/route.ts (new)

**Learnings for future iterations:**
- Route params in Next.js App Router are now async (Promise<{ param: type }>)
- Use x-forwarded-for header (split by comma for first IP) for IP address extraction
- Partner codes tracked via PartnerProfile.referralCodeClickCount, guest codes tracked via ReferralEvent only
- Cookie settings: httpOnly for security, secure in production, sameSite: lax for redirect compatibility
---

## 2026-01-21 - US-002: Referral Attribution at Application
Iteration: 3
- Added referralCode field to Application model in Prisma schema with index
- Modified application.create mutation to read referral_code cookie from headers
- Created ReferralEvent with type APPLICATION when valid referral code exists
- Awards 100 points to referrer: passportPoints for partners, referralPointsBalance for guests
- Sends notification email to partner referrers via existing template
- Created /api/referral/clear-cookie endpoint to delete cookie after successful attribution
- Updated application-form.tsx to call clear-cookie endpoint when referralAttributed is true

Files changed:
- prisma/schema.prisma (added referralCode to Application model)
- lib/trpc/server/routers/application.ts (referral attribution logic)
- components/forms/application-form.tsx (cookie clearing after submission)
- app/api/referral/clear-cookie/route.ts (new)

**Learnings for future iterations:**
- tRPC context includes headers, so cookies can be parsed from ctx.headers.get('cookie')
- httpOnly cookies cannot be cleared from client JS, need server-side endpoint
- Partner referrers receive passportPoints, guest referrers receive referralPointsBalance
- Always validate referral codes against both PartnerProfile and User tables
- Use try/catch around referral logic to not fail the main operation on referral errors
---

## 2026-01-21 - US-003: Referral Attribution at Booking
Iteration: 4
- Modified createPaymentIntent mutation to check for referral_code cookie
- Manual referral code input takes precedence over cookie when both exist
- Added validateAnyReferralCode endpoint to validate both partner and guest codes
- Updated referral-code-input.tsx to accept guest codes (FIRSTNAME-YEAR format)
- Creates ReferralEvent with type BOOKING on successful attribution
- Awards points based on package value: 1000 pts < $15K, 1500 pts >= $15K
- Updates booking.referredBy with the referral code
- Creates GuestReferral record with status BOOKED
- Sends notification email to guest referrer
- Added GUEST_REFERRAL_POINTS_CONFIG and calculateGuestReferralPoints helper
- Created guest-referral-booking.ts email template

Files changed:
- lib/trpc/server/routers/booking.ts (referral attribution logic in createPaymentIntent)
- lib/trpc/server/routers/partner.ts (validateAnyReferralCode endpoint)
- components/booking/referral-code-input.tsx (support both partner and guest codes)
- lib/config/business-constants.ts (GUEST_REFERRAL_POINTS_CONFIG)
- lib/email/templates/guest-referral-booking.ts (new)

**Learnings for future iterations:**
- Booking referral attribution happens in createPaymentIntent, not after payment
- Partner codes provide discounts, guest codes only provide attribution (no discount)
- When checking both partner and guest codes, use Promise.all for parallel queries
- referralAttributed flag is returned to client to indicate cookie should be cleared
- Guest referral points use a different calculation than partner points (flat 1000/1500 vs scaled)
---
